spring:
  application:
    name: greeting-service
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
  endpoint:
    env:
      show-values: ALWAYS
greeting-service:
  greeting: "Ciao"
  default-value: "Mondo"
